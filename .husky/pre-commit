#!/bin/bash
set -e
echo "=== Pre-commit hook starting ==="

# Use npx.cmd if it exists (Windows), otherwise use npx
if command -v npx.cmd &> /dev/null; then
    npx.cmd lint-staged
else
    npx lint-staged
fi